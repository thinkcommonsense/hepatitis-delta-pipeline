// For those of you who wish not to download the data, make sure you --in it.
params {
    accession = "M21012"
    in = "$projectDir/data"
}

process {
    withName: TRIMAL {
        publishDir = [
            path: { "results" },
            mode: 'symlink'
        ]
    }
}


